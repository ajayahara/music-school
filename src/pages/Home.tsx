import { OverView } from "@/app-component/OverView"
import { StudentList } from "@/app-component/StudentList"

export const Home = () => {
  return (
    <div>
        <OverView/>
        <StudentList/>
    </div>
  )
}
